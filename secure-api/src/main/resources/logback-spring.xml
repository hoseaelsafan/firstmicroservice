<configuration>

    <!-- LOG PATTERN WITH requestId -->
    <property name="LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss} [%thread] [%X{requestId}] %-5level %logger - %msg%n"/>

    <!-- APPLICATION LOG -->
    <appender name="APP_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>./secure-api/logs/application.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>./secure-api/logs/application.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- TRANSACTION LOG -->
    <appender name="TRX_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>./secure-api/logs/trx.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>./secure-api/logs/trx.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- ERROR LOG -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>./secure-api/logs/error.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>./secure-api/logs/error.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>ERROR</level>
        </filter>
    </appender>

    <!-- SECURITY LOG -->
    <appender name="SECURITY_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/security.log</file>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- ROOT LOGGER -->
    <root level="INFO">
        <appender-ref ref="APP_FILE"/>
    </root>

    <!-- TRANSACTION LOGGER -->
    <logger name="AUTH_LOGGER" level="INFO" additivity="false">
        <appender-ref ref="TRX_FILE"/>
    </logger>

    <!-- ERROR LOGGER -->
    <logger name="ERROR_LOGGER" level="INFO" additivity="false">
        <appender-ref ref="ERROR_FILE"/>
    </logger>

    <!-- SECURITY LOGGER -->
    <logger name="SECURITY_LOGGER" level="INFO" additivity="false">
        <appender-ref ref="SECURITY_FILE"/>
    </logger>

</configuration>